name: Build Windows executable

on:
  workflow_dispatch:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install uv
        run: python -m pip install uv

      - name: Install and prepare English OCR engine
        run: |
          choco install tesseract --version 5.5.3.20260724 -y --no-progress
          ./scripts/prepare_tesseract.ps1

      - name: Sync locked dependencies
        run: uv sync --locked

      - name: Run tests
        run: uv run pytest -q

      - name: Build portable executable
        run: uv run pyinstaller --noconfirm --clean packaging/windows.spec

      - name: Verify OCR inside packaged executable
        run: ./dist/PDFPageScreenshotCapture.exe --self-test-ocr

      - name: Upload executable
        uses: actions/upload-artifact@v7
        with:
          name: PDFPageScreenshotCapture-windows
          path: dist/PDFPageScreenshotCapture.exe
          if-no-files-found: error
